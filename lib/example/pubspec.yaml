name: glint_example
description: A sample app using the glint package.
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  glint:
    path: ../../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
